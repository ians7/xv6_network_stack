===== 05/29/2025 =====

I've been toying with the idea of writing a network stack for xv6 for a few
days now, and I think that's it's something I really want to take on.
Unfortunately, my final is in about an hour and a half and I'm currently
wasting time that could be spent studying, but I was thinking that journaling
my experience with this project could prove to be very beneficial. I tend to 
get through projects and think "Man, in the end, that was really simple. I 
think that once I got past any of the troubles I had, it was really easy!"
Then I struggle to think about any of the challenges that I faced in during
the project, which isn't good for getting a job! So, I want to journal my
experiences, struggles, successes, things that work, things that don't work,
and so on. Hopefully, this will also lead me to be more invested in the project
so that it's more likely to be successful.

lol

===== 06/03/2025 =====
I've been working on the driver for the virtio-net device for a couple of hours
by now and I think that it might be starting to make a little sense? The docs
have been pretty helpful. 

Reading the block device driver has also been immensely helpful. Having it as a
reference to understand how to actually do everything in code has been
critical, otherwise I would have been having a hard time figuring out how to 
even negotiate the feature bits.

Struggling to access the memory location of the device. I keep getting a
segfault, but I've verified that the device should be located at that
location in memory. Maybe the device isn't even being set up properly?

==== 06/03/2025 ====
Working on it a little while longer, I figured out that I needed to dump
the qemu logs to find that qemu was failing to access 0x1002000. This, 
along with some other info, indicated that the memory was not actually
being mapped to the device. Turns out I had to call `kvmmap` for the
location in memory in vm.c. This makes a lot of sense and I'm kind of
ashamed that I failed to realize this. Doing this fixed my segfault on launch

Worked on this for awhile today, got some decent progress. Need to read a
little bit more about descriptors and how I need to use them. Might have
to draw some things out to really figure out what is happening. 

==== 06/06/2025 ====
Not really sure how to feel about today. I did some reading that (so far) has
been generally unhelpful. I feel like I'm using ChatGPT too much, so I'm trying
to read the documentation a little more, which has been going well enough. It's
just hard when I know little to nothing about drivers and the documentation for
this particular device is only so helpful in regards to that. 

I at least got wireshark set up so that it can sniff the packets that I'm
attempting to transmit, so that's a plus. Unfortunately it can't see any of the
packets that I want to send lol

==== 06/08/2025 ====
After some beating my head into it, I was at a loss for what could have been
going wrong. I was reading through the spec again for the virtio net device
trying to see what I was doing incorrectly. I saw the note (which I've seen
and taken note of several times already) that the MAC feature SHOULD be
negotiated if offered, so I verified with ChatGPT that I was negotiating it
correctly and it turns out that I **wasn't**. I was using the |= operator
instead of the &= operator. This was problematic because I wasn't checking
that the device even offers it, I was just adding it. This messed up how the
device processes my buffers, resulting in failure. After changing this, I
FINALLY got an error that told me the my receive buffers weren't properly
initialized, which appears to be an indication that it's finally processing
my descriptors! Bless up.

Found another bug in the initialization that I identified by reading the
documentation. In section 2.6.5, it says that the header of a receive buffer
must be set to read-only, but I was setting the write-only bit for it.

Absolutely massive bug: QUEUE_RX needs to be 0 and QUEUE_TX needs to be 1, as
per the virtio spec. I did not know this before and I had them swapped. I no
longer get any errors. I'm still not seeing my packets in wireshark, but this
is fine. This feels like a big W

==== 06/14/2025 ====
I forgot to document anything else that I figured out the last few times I've
been working, but I've made significant progress. I figured out that my packets
were not being seen in wireshark because I was setting the hdr_len value. This
value should always be zero unless I'm negotiating packet fragmentation, which
I am not. So, now I am able to see my packets on wireshark

I encountered another issue where I thought that the device was appending 00 00
to the beginning of my packet, but it turns out that this was due to me having
a field in struct virtio_net_hdr that I did not need. Thus, some of the data 
from the header was leaking into my actual packet data. Removing this field 
fixed the issue.

==== 06/28/2025 ====
I haven't had a ton of time to work on this for the past few weeks since I 
started working. Mainly what I have been struggling with is making sure that
my VM can reach the outside internet. At this point, I'm not certain that it 
can reach the outside internet, but it can for sure reach other machines on 
my home network.

I did this by creating a bridge on my machine that connects the VM tap
interface to my ethernet port, allowing the packets from the tap to be sent to
the outside internet through my computer ethernt port. I tested this by making
sure that my laptop can receive packets from my tap interface on my desktop.

Today, I worked for a few hours on researching the linux implementation of 
TCP/IP sockets and defining the API that I will be using. I'm essentially 
creating a copy of the Linux implementation, but I'm not including a lot of the
extra features that I do not need for my simple implementation. I'm doing this
in socket.h and ip.h, separating the IP functionality and the TCP functionality
into these separate header files.

==== 06/29/2025 ====
Mainly what I did today was just clean up some of the types in my header files
and get the syscalls set up. Now, I just need to implement all of them :D

==== 07/20/2025 ====
I've been pecking away at the project for a few weeks now, making a little bit
of progress at a time. Motivation is getting tough at this point between
working full time, going to the gym every day, and other responsibilities. 
Today I managed to finish up my implementation of `socket()`. I've also 
been creating a number of other structs and things that will be important for
my net stack. ChatGPT has been a vital help to the project at this point. I've
been using it as someone to bounce ideas off of and learn some of the practices
that are used in the linux kernel implementation.

==== 08/25/2025 ====
I went of vacation for a couple weeks, so I wasn't able to work on the project
during that time. Since getting back, I've worked on it pretty intensely for a 
few days. I created the ethernet, tcp, and ip parsing functions.

These went pretty smoothly, I utilized Chatgpt to do the work of filling in the
struct fields so that I wouldn't have to type it all out. I separated the proto
headers and frames (hdr + payload) to make sure that all of the data in the 
headers would be packed together in memory, and the payload can be stored
however the OS wants to store it since it could potentially be a huge chunk of
memory.

I'm now working on building and transmitting packets. I forsee this being a 
time consiming process making sure that the functions can access all of the
information that they need to craft packets. I'm mainly making sure that my
system is capable of the handshake before implementing any user `send()` or 
`recv()` functions.

==== 08/26/2025 ====
I put in a ton of work today. Basically, all I did today was implement ARP
and separate all of the protocol code from virtio_net.c to their own files.
I've done all of ARP by now. I found that I created a bug in my code where
I was running out of descriptors (alloc_desc returned -1) and I was not
checking for this condition. I decided that I would simply start dropping
packets when I run out of descriptors, this being a limitation to my system.

Next, I'd like to start implementing UDP sockets. I wanted to go straight to 
TCP sockets, but UDP will be a good place to start.

==== 08/28/2025 ====
Just putting in the work to implement udp. made some things a little more
flexible for tcp/udp by making udp and tcp version of the socket api methods,
and then the api methods call the correct one from a function pointer in the
struct socket.

I've gotten around to implementing udp_bind, but I still need to implement
udp_close. Before working on that though, I'm going to work on implementing
the send, recv, sendto, and recvfrom syscalls. close isn't currently imprtant
for getting things up and running, so that is going to be a problem for future
me.

I can successfully launch a UDP server and read the data from another machine on
my network! Some issues that I encountered: I passed along incorrect header 
data, which I was able to debug with wireshark (to verify the packet) and
auditing my code path.

I also had trouble with how I wanted to separate fucntionality between my
syscalls and my api. Ultimately, it came down to design decision, but I 
bounced my ideas off of chatgpt and asked about typical linux kernel practice

==== 09/02/2025 ====
I've been working on udp_sendto. it seems to be going okay, but I think that I
might be building the packets incorrectly. The udp server machine receives the
datagrams (proven by wireshark), but I suspect that the datagrams are malformed
because the server application is not receiving them. Unfortunately the summer
is now over, and I will likely not be able to work on the project any longer. I 
will probably write a reflection sometime soon so that I can talk about it at
the career fair and whatnot
